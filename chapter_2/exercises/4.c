//Write a program that declares int and floats without initializing them and pritns thier values
//Look for patterns if there are any (usually there isn't)

#include <stdio.h>

int main(void) {
    int a;
    int b;
    float c;
    float d;

    printf("This is a: %d, this is b: %d, this  is c: %f, this is d: %f", a, b, c, d);
    return 0;
}